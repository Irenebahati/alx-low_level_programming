#include <stdio.h>
/**
 * main - Beginning of program
 * Return: 0
 */

int main(void)
{
	char A = 'a';
	int B = 0;

	while
		(B < 10) {
			putchar(B + '0');
			B++;
		}
	while
		(A <= 'f') {
			putchar(A);
			A++;
		}
	putchar('\n');
	return (0);
}
